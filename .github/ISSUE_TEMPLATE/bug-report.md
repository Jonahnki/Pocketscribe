---
name: Bug report
about: Something went wrong, or Pocketscribe reported something that looks incorrect
title: "[bug] "
labels: ["bug"]
---

## What happened

## What you expected instead

## The command you ran

```
pocketscribe ...
```

## Environment

- Pocketscribe version (`pocketscribe version`):
- Python version:
- Operating system:
- fpocket installed? (`fpocket --version`):

## Input structure

- **Which tool produced it?** (AlphaFold2, ColabFold, ESMFold, ...)
- **Where did it come from?** (AlphaFold DB download, a Colab notebook, a local run, ...)
- **Roughly how large?** (residues, chains)

If you can share the file, please attach it — bugs in parsing and confidence extraction
are very hard to reproduce without one. If the structure is unpublished, a small
truncated version that still reproduces the problem is just as useful.

## Full output

Please paste the complete terminal output, including the error message.

```

```

## If a result looks wrong rather than crashing

Please say which number or statement looks wrong and what you expected. For pocket or
confidence results, attaching the generated HTML report is the most useful thing you can
include.
